---
description: CHANGELOG.md 写作规范 - 区分用户内容和技术细节
globs: CHANGELOG.md
alwaysApply: false
---

# CHANGELOG 写作规范

系统会自动将 CHANGELOG 最新版本推送给用户，**只推送 `### 本次更新` 部分**。

## 版本格式

```markdown
## v1.x.x - 2026年X月X日

### 本次更新

（用户会看到的内容）

### 技术改进

（用户不会看到）

### 运营须知

（用户不会看到）

---
```

## 章节用途

| 章节 | 推送给用户 | 写作口吻 |
|------|-----------|---------|
| `### 本次更新` | ✅ 是 | 面向用户，简洁易懂 |
| `### 技术改进` | ❌ 否 | 技术细节，代码变更 |
| `### 测试验证` | ❌ 否 | 测试结果，开发记录 |
| `### 运营须知` | ❌ 否 | 内部运营信息 |

## ✅ 正确示例

```markdown
### 本次更新

**多语言自适应**
- Bot 现在会自动识别你的语言设置
- 支持中文、英文、日文、韩文

### 技术改进

- 新增 `locales/ui_strings.py` 管理 UI 文案
- 用户语言存储在 `users.json` 的 `language` 字段
```

## ❌ 错误示例

```markdown
### 本次更新

- 新增 `locales/ui_strings.py` 文件  ← 用户不关心文件名
- 修复了 `get_user_language()` 返回 None 的 bug  ← 技术细节
```

## 写作原则

1. **用户视角**：描述用户能感知到的变化
2. **简洁明了**：避免技术术语
3. **功能导向**：说明「能做什么」而非「怎么做的」
