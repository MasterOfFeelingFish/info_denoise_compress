# CHANGELOG.md 写作规范

## 适用范围
当编辑 `CHANGELOG.md` 文件时，必须遵循以下规范。

## 受众区分原则

CHANGELOG.md 有两类读者，内容必须按读者分开：

### `### 本次更新` - 给用户看的
- **语言**：用户能理解的日常语言
- **内容**：用户能感知到的功能变化
- **禁止**：
  - 技术术语（temperature、API、环境变量等）
  - 代码/文件名（`config.py`、`language_service.py`）
  - 开发概念（缓存、版本号机制、占位符）

**示例对比**：
| ❌ 错误（技术视角） | ✅ 正确（用户视角） |
|-------------------|-------------------|
| 新增 `TRANSLATION_TEMPERATURE` 环境变量 | 翻译质量更稳定准确 |
| 修改 onboarding prompt 支持动态语言 | 对话现在完全使用你的语言 |
| 新增 UI 缓存版本号机制 | （不写，用户感知不到） |
| 支持任意语言的用户 | 支持更多语言：俄语、法语等 |

### `### 技术改进` - 给开发者看的
- **语言**：技术术语、代码引用
- **内容**：架构变更、新增文件、配置项
- **格式**：可以使用代码块、文件路径

### `### 测试验证` - 给开发者看的
- **内容**：测试结果、通过率
- **格式**：代码块展示测试输出

## 写作检查清单

编辑 `### 本次更新` 前，问自己：
1. 这句话普通用户能理解吗？
2. 这个功能用户能直接感知到吗？
3. 有没有技术术语需要换成日常语言？

如果不确定某内容属于哪个部分，默认放到 `### 技术改进`。
